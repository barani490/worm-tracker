# Core dependencies (required for --mode analyze on any machine)
opencv-python>=4.5
numpy>=1.21

# Optional: only needed if you want the auto-generated summary plot
# (--mode analyze fails soft and skips the plot if this isn't installed)
matplotlib>=3.4

# Optional: only needed on a Raspberry Pi for --mode record / record-analyze / live / still
# (not installable/usable on a non-Pi machine -- do not install this on a dev laptop)
# picamera2
